# T12.7 — Webhook HTTP regression hardening (done)

**Status:** Complete. **252** pytest green.

## Summary

- Webhook route: `TenantContextError` → HTTP 400, `VALIDATION_ERROR`, generic message (no ID leak)
- `tests/test_t12_webhook_http_regression.py` — HTTP envelope, duplicate, AI failure, tenant error, follow-up
- `tests/test_t11_ai_webhook_integration.py` — duplicate lead flags, `ai_failure` notify, urgent active-lead

---

@alpstein-backend-engineer

Implement T12.7 only.

Goal:
Harden the completed T12 webhook lead/notification integration before moving to n8n workflows.

Scope:
- integration/regression hardening only
- small error-mapping cleanup if needed
- no new product features
- no n8n workflows
- no DB schema changes
- no AI service changes
- no notification delivery
- no CRM routing/scoring/assignment

Focus areas:

1. HTTP-level regression tests
- exercise POST /api/v1/webhook/message through the route
- include valid X-Alpstein-Webhook-Token
- verify response envelope contains:
  - reply_to_customer
  - lead_created
  - lead_updated
  - lead
  - notify_owner
  - notification when applicable
  - conversation
  - message
- verify no final_prompt/system_prompt/raw_payload/internal fields exposed

2. Duplicate HTTP behavior
- duplicate incoming webhook returns:
  - lead_created=false
  - lead_updated=false
  - notify_owner=false
  - no notification object
  - no second lead write
  - no second outgoing AI message

3. AI failure HTTP behavior
- gateway failure/fallback path returns:
  - fallback reply_to_customer
  - lead_created or lead_updated according to lead state
  - notify_owner=true
  - notification.type=ai_failure

4. TenantContextError HTTP handling
- if tenant/business/customer/conversation mismatch happens:
  - return standard error envelope
  - use 4xx status, not unhandled 500
  - do not leak internal IDs/secrets
- keep route thin

5. Existing tests
- strengthen T11/T12 integration tests only where they currently miss:
  - duplicate lead flags
  - ai_failure notification
  - urgent/handoff on active lead if easy

Rules:
- do not change core orchestration behavior unless a test exposes a real bug
- no new response fields
- no n8n changes
- no delivery integrations
- no retries/queues
- no new abstractions

After implementation:
- create/move task file according to AGENTS.md lifecycle rules
- run targeted tests and full suite
- update completed.md
- update current-state.md
- update next-steps.md
- stop for review