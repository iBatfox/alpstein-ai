# T13.0-impl — Alpstein n8n Docker Compose

**Status:** done (implementation committed) — **runtime not started**; operator review + `docker-compose up` pending  
**Depends on:** [`docs/ops/n8n-deployment-plan.md`](../../docs/ops/n8n-deployment-plan.md) (T13.0 plan) accepted  
**Skill:** alpstein-n8n-integration-engineer  
**Runbook:** [`docs/ops/n8n-runtime-start.md`](../../docs/ops/n8n-runtime-start.md) — use **`docker-compose`** (v1.29 on this server)

## Goal

Deploy project-local n8n container `alpstein_n8n` for T13 workflow runtime and Gate 1 verification.

## Scope (committed)

- [x] `n8n/docker-compose.yml`
- [x] `n8n/.env.example` (names only)
- [x] `.gitignore` entry for `n8n/.env`
- [x] [`docs/ops/n8n-runtime-start.md`](../../docs/ops/n8n-runtime-start.md)
- [x] `docker-compose -p alpstein-n8n config` validates on server
- **No** workflow JSON changes
- **No** backend code
- **No** modification to `integrationhubspot_n8n`

## Requirements

| Item | Spec |
|------|------|
| Compose path | `/opt/alpstein-ai/n8n/docker-compose.yml` |
| Env file | `/opt/alpstein-ai/n8n/.env` (operator-created, not committed) |
| Container name | `alpstein_n8n` |
| Compose project | `alpstein-n8n` |
| Host port | `127.0.0.1:15679:5678` |
| Volume | named `alpstein_n8n_data` → `/home/node/.n8n` |
| Backend URL (dev) | `BACKEND_BASE_URL=http://host.docker.internal:8000` — **not** `127.0.0.1` inside container |
| Auth | `N8N_BASIC_AUTH_*` required |
| Encryption | `N8N_ENCRYPTION_KEY` required before first start |
| Integration env | `N8N_BACKEND_API_TOKEN`, optional `ALPSTEIN_TEST_BUSINESS_ID` |

## Done when

### Implementation (committed)

- [x] `n8n/docker-compose.yml` + `n8n/.env.example` + `.gitignore`
- [x] Runtime runbook with `docker-compose` commands
- [x] `docker-compose -p alpstein-n8n config` valid
- [x] Port **15679** free; no change to `integrationhubspot_n8n` during impl

### Runtime (operator — pending)

- [ ] `docker-compose -p alpstein-n8n up -d` from `n8n/` starts `alpstein_n8n`
- [ ] `integrationhubspot_n8n` still running on `15678`
- [ ] UI reachable at `http://127.0.0.1:15679` with basic auth
- [ ] Manual verification §13 in deployment plan passes
- [ ] Gate 1 runnable after workflow import

## Out of scope

- nginx / TLS / `n8n.alpstein-ai.ch`
- Root `docker-compose.yml` full stack
- WhatsApp, Telegram credentials
- Workflow logic changes

## Reference

- Plan: [`docs/ops/n8n-deployment-plan.md`](../../docs/ops/n8n-deployment-plan.md)
- Env checklist: [`docs/ops/n8n-env-credential-checklist.md`](../../docs/ops/n8n-env-credential-checklist.md)
