# T13.4 — Customer reply response shaping

**Status:** done (2026-05-25)  
**Skill:** alpstein-n8n-integration-engineer

## Goal

Reshape test-channel webhook success response to customer-facing JSON using `data.reply_to_customer` only.

## Done when

- [x] **Shape Customer Reply** node between POST Backend and respond node
- [x] Success: `{ "success": true, "reply_to_customer": "..." }` (+ optional `message.is_duplicate`)
- [x] No lead/notification/raw envelope in webhook response
- [x] HTTP error path unchanged (502 + `N8N_BACKEND_REQUEST_FAILED`)
- [x] Manual test: new + duplicate `external_message_id`
- [x] Export: `n8n/workflows/t13_workflow1_test_webhook_skeleton.json`
- [x] Docs: `docs/ops/n8n-workflow1-test-webhook.md`, project-status, slice

## Out of scope (honored)

- Backend changes
- Telegram / notify branch
- Retries
- WhatsApp
- Production webhook policy / nginx / docker changes in repo

## Manual test (2026-05-25)

| Case | HTTP | Body |
|------|------|------|
| New `external_message_id` | 200 | `success: true`, non-empty `reply_to_customer`, `message.is_duplicate: false` |
| Duplicate same id | 200 | `success: true`, `reply_to_customer` present, `message.is_duplicate: true` |

Endpoint: `http://127.0.0.1:15679/webhook/alpstein/test/incoming` (workflow active in runtime).

## Next

**T13.5** — `notify_owner` + Telegram owner notification branch.
