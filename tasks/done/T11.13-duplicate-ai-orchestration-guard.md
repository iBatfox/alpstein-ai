@alpstein-backend-engineer

Implement T11.13 only.

Goal:
Prevent duplicate incoming webhook retries from executing the AI reply chain multiple times.

Scope:
- duplicate-message orchestration rule only
- no webhook route integration yet
- no outgoing message persistence
- no n8n changes
- no lead/notification logic

Requirements:
- add orchestration-level duplicate handling
- use existing MessageService duplicate detection result (is_duplicate)
- when incoming customer message is duplicate:
  - skip AI orchestration chain entirely
  - skip PromptRun creation
  - return typed duplicate/no-op result
- add typed DTO/result if needed:
  - is_duplicate
  - ai_executed
  - reason
- preserve existing non-duplicate behavior

Rules:
- no OpenAI/provider calls for duplicates
- no Prompt Builder execution for duplicates
- no PromptRun persistence for duplicates
- no outgoing AI message persistence
- no webhook/API response changes yet unless already required internally
- no DB schema changes

Tests:
- duplicate incoming message skips AI chain
- duplicate incoming message skips PromptRun
- duplicate result DTO is returned
- non-duplicate path still executes normally
- no provider calls occur for duplicates

After implementation:
- create/move task file according to AGENTS.md lifecycle rules
- run tests
- update completed.md
- update current-state.md
- update next-steps.md
- stop for review