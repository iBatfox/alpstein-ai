# T12.2 — LeadService (done)

**Status:** Complete. **204** pytest green.

## Summary

- `LeadService` in `app/services/lead_service.py`
- `find_active_lead` — scoped lookup; active statuses only; latest by `updated_at` then `created_at`
- `create_lead` — defaults `new` / `normal`; `add` + `flush` only
- `update_lead` — allowed MVP fields; tenant/business scope check; `updated_at` touch; `flush` only
- Tests: `tests/test_lead_service.py` (10 tests)

---

@alpstein-backend-engineer

Implement T12.2 only.

Goal:
Add LeadService for MVP lead persistence/update logic.

Read first:
- specs/database/database-schema.md
- specs/database/entities.md
- specs/flows/lead-creation-flow.md
- docs/project-status/t12-lead-notification-plan.md

Scope:
- LeadService only
- no webhook wiring yet
- no notify_owner logic yet
- no n8n
- no AI changes
- no routing/assignment/scoring

Required methods:
1. find_active_lead(...)
- filters:
  tenant_id
  business_id
  customer_id
  conversation_id
- active statuses only:
  new
  in_progress
  contacted
- exclude:
  closed
  lost
- return latest matching active lead or None

2. create_lead(...)
- create new lead row
- required tenant/business/customer/conversation scope
- default:
  status="new"
  priority="normal"
- source_channel from conversation/message context
- add + flush only (no commit)

3. update_lead(...)
- update allowed MVP fields only:
  ai_summary
  customer_note
  service_requested
  preferred_date
  preferred_time
  priority
  status
- update updated_at
- no pipeline/routing logic

Rules:
- all queries tenant-scoped
- no repository layer
- use existing AsyncSession service style
- no webhook/business orchestration in this task
- no duplicate prevention here beyond active-lead lookup
- no notify_owner logic
- no OpenAI/provider logic

Tests:
- active lead lookup
- closed/lost excluded
- latest active lead selected
- create_lead defaults
- update_lead updates only allowed fields
- tenant/business filters enforced
- no commit inside service

After implementation:
- create/move task file according to AGENTS.md lifecycle rules
- run tests
- update completed.md
- update current-state.md
- update next-steps.md
- stop for review