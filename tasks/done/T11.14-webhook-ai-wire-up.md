# T11.14 — Webhook AI wire-up

**Status:** done

**Goal:** Replace T10 `STUB_REPLY_TO_CUSTOMER` in `WebhookMessageService` with the T11 AI stack.

**Implementation:** `WebhookMessageService.process_incoming_message` calls `AiReplyOrchestrationCoordinator` with `save_result.is_duplicate`, then resolves `reply_to_customer` and optionally persists outgoing AI messages via `MessageService.save_outgoing_ai_message`. On AI failure, `AiReplyFallbackService` supplies fallback text.

**Duplicate path:** coordinator skips AI chain; no new outgoing AI message; `reply_to_customer` from `MessageService.find_last_outgoing_ai_message` or `DUPLICATE_SAFE_ACKNOWLEDGMENT`.

**Route:** unchanged envelope; commit/rollback remain route-owned.
