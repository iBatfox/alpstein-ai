# T13.5 — Owner notification branch

**Status:** done — runtime delivery verified (2026-05-25)  
**Skill:** alpstein-n8n-integration-engineer

## Goal

Telegram owner notification when backend sets `notify_owner` and `notification`.

## Implementation

- **IF Notify Owner** — `$('POST Backend').first().json.data.notify_owner`, `data.notification` not empty, `data.message.is_duplicate === false`
- **Shape Owner Notification** — Telegram-safe text (type, priority, reason, customer message, lead/conversation status; no UUIDs)
- **Telegram Owner Notify** — credential name `Telegram account`, `$env.TELEGRAM_CHAT_ID`, `continueOnFail: true`
- **Topology:** POST Backend → Shape Customer Reply → Respond Customer Reply → IF → Shape Owner → Telegram

## Runtime verification (2026-05-25) — **passed**

**Workflow:** `2qfhWKtgbDy6YeTh` (active)  
**Endpoint:** `POST http://127.0.0.1:15679/webhook/alpstein/test/incoming`  
**Credential:** n8n `Telegram account` (`telegramApi`)  
**Env:** `TELEGRAM_CHAT_ID` in `n8n/.env` (gitignored; not committed)

| Scenario | Exec ID | HTTP | Shaped reply | Notify branch | Telegram |
|----------|---------|------|--------------|---------------|----------|
| **A** Normal follow-up | 52 | 200 | `success`, `reply_to_customer` | Skipped | — |
| **B** Urgent | 50 | 200 | `success`, `reply_to_customer` | Full post-Respond chain | **Delivered** (`message_id` in execution) |
| **C** Duplicate | 51 | 200 | `is_duplicate: true` | IF only (no Shape/Telegram) | Suppressed |

**Execution 50 node order (event log):** Test Webhook → Normalize → POST Backend → Shape Customer Reply → Respond Customer Reply → IF Notify Owner → Shape Owner Notification → Telegram Owner Notify (started/finished).

**Gate 2 (owner notify):** **Passed**

**Live Telegram customer flow:** **May start** — test-channel owner notify path verified; use production channel checklist before customer-facing Telegram ingress.

## Next

Production Telegram customer ingress + T13.6 (errors/retries) when scheduled.
