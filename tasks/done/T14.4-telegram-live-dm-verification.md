# T14.4 — Live Telegram DM verification

**Status:** **passed (pipeline)** — customer Telegram API delivery pending operator DM to `@alpsteinai_0001bot`  
**Date:** 2026-05-25  
**Workflow (runtime):** `alpstein-incoming-message-telegram` — id `zeE8EiOPVjhtiCz1`  
**Repo export:** still `active: false` — runtime was activated for controlled test only

## Preconditions verified

| Check | Result |
|-------|--------|
| Only one Telegram Trigger workflow for customer bot | Pass — single active instance during test (`zeE8EiOPVjhtiCz1`); duplicate imports inactive |
| Customer credential on Trigger/Send | Pass — `alpsteinai_0001bot` (`id=vQ2pf2xcR7BryDFy`) in DB |
| Owner credential on Owner Notify | Pass — `AlpsteinAIbot` |
| Backend health | Pass (200) |
| Customer bot webhook host | `n8n.alpstein-ai.ch` (public) |

## Test method

Controlled **private-message** updates were injected to the n8n Telegram webhook path (`alpstein-telegram-customer-trigger/webhook`) with the same Update shape Telegram uses. Synthetic `chat_id` suffix `***1001` was used — **not** a real user chat, so Telegram Send correctly returned `chat not found` while the backend/AI path still ran.

**Operator live DM:** Send the test phrase to `@alpsteinai_0001bot` in Telegram after re-activating `zeE8EiOPVjhtiCz1` to confirm delivery in a real chat (webhook was cleared post-test).

## Execution evidence

| Scenario | Exec ID | Workflow status | Nodes (summary) | Backend | Owner notify |
|----------|---------|-----------------|-----------------|---------|--------------|
| Normal — *"Hi, do you have an appointment today?"* | **55** | success | Trigger → Normalize → POST Backend → Shape → Send; IF → Shape Owner → Owner Notify | `success: true`, `reply_to_customer` present | **Delivered** (Telegram API ok) |
| Urgent — *"URGENT: I need an appointment ASAP"* | **56** | success | Same chain; owner branch ran | `success: true`, `notify_owner: true` | Branch ran (`urgent_lead` copy in execution) |
| Duplicate — same `tg:***:44001` | **57** | success | Trigger → Normalize → POST Backend → Shape → Send; IF only (no Owner Notify) | `success: true`, `is_duplicate: true` | **Skipped** (expected) |

### Customer reply (Telegram Send Message)

| Exec | Telegram API | Notes |
|------|--------------|-------|
| 55–57 | `chat not found` | Expected with synthetic chat; proves Send node ran with `telegram_chat_id` from normalize, not `TELEGRAM_CHAT_ID` |

### Parallel topology confirmed

POST Backend **success** fans out to **Shape → Send** and **IF Notify Owner** as siblings (not Send → IF). Exec **57** shows owner branch omitted when `is_duplicate: true`.

## Post-test runtime state

- Workflow **deactivated** (`zeE8EiOPVjhtiCz1`, `active=false` after restart)
- Customer bot `deleteWebhook` called to allow future `getUpdates` / operator re-activation
- **No** secrets or chat IDs logged in this doc

## Verdict

| Layer | Operational? |
|-------|----------------|
| Trigger → Normalize → POST Backend → Shape | **Yes** |
| AI `reply_to_customer` returned | **Yes** |
| Owner notify (real `TELEGRAM_CHAT_ID`) | **Yes** on normal; urgent branch executed |
| Duplicate / idempotency | **Yes** (exec 57) |
| Customer Telegram delivery to real user DM | **Pending** — requires real private chat to `@alpsteinai_0001bot` |

## T14.5 gate

**T14.5 MAY START** for owner-notify + duplicate regression on Telegram path, with the understanding that **full customer DM delivery** should be confirmed when an operator sends the test phrase to `@alpsteinai_0001bot` after re-activation.
