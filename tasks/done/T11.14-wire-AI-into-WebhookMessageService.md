@alpstein-backend-engineer

Implement T11.14 only.

Goal:
Replace T10 stub reply_to_customer in WebhookMessageService with the T11 AI orchestration stack.

Use existing components:
- MessageService.save_incoming_customer_message
- AiReplyOrchestrationCoordinator from T11.13
- AiReplyOrchestrationService from T11.11
- AiReplyFallbackService from T11.12
- MessageService.save_outgoing_ai_message from T11.9

Requirements:
- WebhookMessageService should call AI orchestration after incoming message save
- pass save_result.is_duplicate into AiReplyOrchestrationCoordinator
- if duplicate:
  - skip AI execution
  - do not create PromptRun
  - do not save new outgoing AI message
  - reply_to_customer should use safe duplicate response strategy:
    - prefer last outgoing AI message if already available, or
    - safe acknowledgment fallback
- if non-duplicate and AI succeeds:
  - use AiReplyResult.text as reply_to_customer
  - save outgoing AI message
- if non-duplicate and AI fails:
  - apply AiReplyFallbackService
  - use fallback_text as reply_to_customer when should_reply=true
  - save fallback outgoing AI message if reply is sent
- keep lead_created=false
- keep notify_owner=false for now
- route remains thin
- commit/rollback behavior remains route-owned
- no n8n changes
- no lead/notification logic
- no migrations
- no retries

Rules:
- WebhookMessageService may orchestrate but must not build prompts itself
- WebhookMessageService must not call OpenAI directly
- WebhookMessageService must not perform raw SQL
- final_prompt must not appear in API response
- system prompts must not appear in API response

Tests:
- non-duplicate successful AI returns real AI text instead of stub
- outgoing AI message saved on successful AI reply
- AI failure uses fallback text
- fallback outgoing message saved when fallback reply is sent
- duplicate skips AI coordinator execution
- duplicate does not create PromptRun / outgoing AI message
- duplicate response uses safe duplicate strategy
- response still includes lead_created=false and notify_owner=false
- final_prompt/system prompts are not exposed
- route remains thin / response envelope unchanged

After implementation:
- create/move task file according to AGENTS.md lifecycle rules
- run tests
- update completed.md
- update current-state.md
- update next-steps.md
- stop for review