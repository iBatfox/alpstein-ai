@alpstein-ai-integration-engineer

Implement T11.4 only.

Goal:
Add AI Configuration Service that loads the tenant/business AI configuration needed for prompt building.

Requirements:
- create AI Configuration Service using existing T11.2 read services
- load:
  - tenant_business_profile
  - tenant_ai_profile
  - tenant_channel_setting
  - active prompt_template by template_key
- return a typed/config DTO for downstream Prompt Builder
- enforce platform-over-tenant boundary:
  - prompt_template/system prompt is platform-controlled
  - tenant config may provide business context/style only
  - tenant config must not override platform safety/system prompt
- allow optional missing tenant configs where MVP can fallback safely
- no Prompt Builder yet
- no Knowledge Retrieval yet beyond existing read helper
- no AI Gateway
- no OpenAI calls
- no webhook route changes
- no DB writes
- no repositories
- no migrations

Tests:
- loads full config for tenant/business/channel/template_key
- queries remain tenant_id + business_id scoped
- missing optional tenant config returns safe DTO values
- missing prompt_template fails clearly
- tenant config cannot replace platform system prompt
- no provider calls

After implementation:
- run tests
- update completed.md
- update current-state.md
- update next-steps.md
- stop for review