Fix backend PostgreSQL auth blocker for Alpstein AI runtime on :8010.

Context:
T13.5 n8n workflow implementation passed review, but runtime tests are blocked because POST /api/v1/webhook/message returns 500 with PostgreSQL InvalidPasswordError on the backend instance bound to :8010.

Scope:
- backend runtime configuration only
- Docker/systemd/env verification only
- no application logic changes unless strictly required to make existing config load correctly
- no DB schema changes
- no migrations
- no n8n workflow changes
- no Telegram changes
- no secrets committed
- no secrets printed in logs or docs

Goal:
Make the backend instance used by n8n on :8010 connect successfully to PostgreSQL.

Tasks:
1. Identify how the backend on :8010 is started:
   - docker compose
   - systemd
   - uvicorn process
   - other

2. Identify which env var it uses for PostgreSQL connection:
   - DATABASE_URL
   - ALPSTEIN_AI_DATABASE_URL
   - POSTGRES_* vars
   - project-specific settings

3. Compare runtime env with the actual PostgreSQL credentials.

4. Fix the mismatch safely:
   - update local runtime env only
   - do not commit secrets
   - keep .env.example placeholder-only

5. Restart only the affected backend service.

6. Verify:
   - GET /api/v1/health returns 200
   - direct POST /api/v1/webhook/message returns 200 using valid test payload and X-Alpstein-Webhook-Token
   - no InvalidPasswordError remains in logs
   - pytest still passes if code was touched

7. Update docs/status only if needed:
   - document the cause without secrets
   - document verification commands with placeholders

Deliverables:
- root cause summary
- files changed
- commands run
- verification output
- confirmation whether T13.5 runtime tests may continue

Important:
Do not change n8n workflow.
Do not start T13.6.
Do not expose passwords/tokens.