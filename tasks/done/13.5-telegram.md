Implement T13.5 owner notification branch.

Scope:
- n8n workflow only
- workflow JSON export
- docs/status updates for T13.5
- no backend changes
- no DB changes
- no retries
- no WhatsApp
- no multi-client routing
- no AI prompt changes

Goal:
Add Telegram owner notification branch based on backend response.

Requirements:

1. Keep existing customer reply flow unchanged:

POST Backend
→ Shape Customer Reply
→ Respond Customer Reply

T13.4 response shaping must remain intact.

2. Add separate notify branch from POST Backend success output.

Important:
Notify branch MUST read from POST Backend output directly.
Do NOT read notify data from Shape Customer Reply.

3. Add IF node:

Condition:
data.notify_owner === true

and
data.notification exists

4. Add Shape Owner Notification node.

Map backend notification payload into Telegram-safe text.

Include:
- notification_type
- customer message text if available
- lead status if available
- short conversation context if available

Do not expose internal IDs or raw backend envelope.

5. Add Telegram Send Message node.

Use placeholder/safe credential references only.
No secrets in export.

6. Duplicate behavior:

Duplicate messages must NOT create additional owner notifications.

7. Error handling:

Existing backend error branch must remain unchanged:

502
N8N_BACKEND_REQUEST_FAILED

8. Workflow export:

active=false is intentional.

9. Update docs/status:

- mark T13.5 done
- next task:
  real Telegram customer flow/live channel testing

Deliverables:
- updated workflow export
- updated docs
- short runtime test summary

Manual test scenarios:

1. Normal customer message:
- customer reply returned
- no owner Telegram notification

2. Urgent/handoff message:
- customer reply returned
- owner Telegram notification sent

3. Duplicate message:
- duplicate=true
- no additional owner notification