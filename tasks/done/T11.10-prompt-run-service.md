@alpstein-backend-engineer

Implement T11.10 only.

Goal:
Add PromptRunService for persisting AI execution logs.

Requirements:
- create PromptRunService
- add method to create prompt_run records for AI execution
- persist:
  - tenant_id
  - business_id
  - conversation_id
  - message_id
  - prompt_template_id
  - prompt_version
  - model
  - provider
  - input_tokens
  - output_tokens
  - latency_ms
  - result/status
  - error
  - final_prompt only if already supported by schema, with safe truncation/redaction
- support both success and failure records
- validate tenant/business/conversation/message consistency where possible
- use AsyncSession pattern
- session.add + flush only
- no commit inside service

Rules:
- no AI Gateway calls
- no Prompt Builder calls
- no webhook orchestration
- no message persistence changes
- no migrations unless existing schema blocks PromptRunService
- no secrets or raw API keys in prompt_runs
- do not expose final_prompt through any API

Tests:
- creates success prompt_run
- creates failure prompt_run
- stores token/latency/model/provider fields
- truncates/redacts final_prompt if applicable
- does not store secrets/API keys
- uses add + flush only, no commit
- validates tenant/message/conversation scope where possible

After implementation:
- create/move task file according to AGENTS.md lifecycle rules
- run tests
- update completed.md
- update current-state.md
- update next-steps.md
- stop for review