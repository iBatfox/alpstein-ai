@alpstein-backend-engineer

Implement T11.4 test hardening only.

Scope:
- update backend/tests/test_ai_configuration_service.py
- do not change production logic unless a test exposes a real bug
- do not change routes, DB, migrations, Prompt Builder, Gateway, or orchestration

Add tests for:
1. mis-scoped tenant_business_profile raises TenantContextError
2. mis-scoped tenant_ai_profile raises TenantContextError
3. mis-scoped tenant_channel_setting raises TenantContextError
4. channel_setting.channel mismatch raises TenantContextError

Optional:
- assert metadata scrub applies to business_context and channel_rules, not only behavior

After implementation:
- run tests
- update completed.md only if needed
- stop for review