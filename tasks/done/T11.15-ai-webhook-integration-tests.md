# T11.15 — AI webhook integration/regression tests

**Status:** done

**Goal:** Regression coverage for the full T11 AI webhook path with mocked provider (no real OpenAI).

**Tests:** `backend/tests/test_t11_ai_webhook_integration.py`

**Scenarios:**

1. Non-duplicate success — AI chain, PromptRun, outgoing AI message, real reply text
2. Gateway failure — PromptRun with error, fallback outgoing message
3. Duplicate — skip AI/PromptRun/new outgoing; last AI reply or safe acknowledgment
4. Leakage — API envelope excludes `final_prompt`, `system_prompt`, `raw_payload`
5. Tenant isolation — config/knowledge scoped; mis-scoped knowledge raises `TenantContextError`

**Also:** `test_webhook_route_module_stays_thin` — route has no OpenAI/Prompt Builder imports.
