@alpstein-backend-engineer

Implement T11.11 only.

Goal:
Add AiReplyOrchestrationService that chains existing AI services and returns an AI reply result.

Use existing components:
- AiConfigurationService from T11.4
- KnowledgeRetrievalService from T11.5
- MessageService.load_recent_conversation_history from T11.6
- PromptBuilderService from T11.7
- AiGatewayService from T11.8
- PromptRunService from T11.10

Requirements:
- create AiReplyOrchestrationService
- input should include tenant_id, business_id, conversation, incoming_message/customer message text, channel, task/template key
- load AI configuration
- retrieve knowledge
- load conversation history
- build AssembledPrompt
- call AiGatewayService
- create PromptRun for success or failure
- return typed AiReplyResult:
  - text
  - is_success
  - prompt_run_id
  - model
  - provider
  - error
- no outgoing AI message persistence yet unless already planned for this slice
- no webhook route changes
- no n8n changes
- no lead/notification logic
- no retries
- no fallback policy yet if T11.12 owns fallback

Rules:
- orchestrator may coordinate services but must not build prompts itself
- orchestrator must not call OpenAI directly
- orchestrator must not perform raw SQL
- PromptRun must be written even on Gateway failure
- final_prompt stored through PromptRunService with existing redaction/truncation
- do not expose final_prompt in result DTO

Tests:
- successful AI reply chain calls services in correct order
- Gateway success creates successful PromptRun
- Gateway failure creates failure PromptRun
- Prompt Builder output is passed to Gateway
- orchestrator does not call OpenAI directly
- no webhook/route side effects
- result DTO does not expose final_prompt

After implementation:
- create/move task file according to AGENTS.md lifecycle rules
- run tests
- update completed.md
- update current-state.md
- update next-steps.md
- stop for review