# T14.2 — Telegram customer credential verification

**Status:** done  
**Date:** 2026-05-25  
**Skill:** alpstein-n8n-integration-engineer  
**Parent plan:** [`t14-telegram-customer-ingress.md`](../todo/t14-telegram-customer-ingress.md)

## Goal

Separate **customer** Telegram bot credential in n8n; verify via `getMe` it is **not** the owner-notify bot. No Trigger workflow (T14.3).

## Requirements checklist

| # | Requirement | Status |
|---|-------------|--------|
| 1 | Customer bot separate from owner notify | **Pass** |
| 2 | Distinct n8n credentials (operator-named) | **Pass** — see table below |
| 3 | Token only in n8n encrypted store | **Pass** |
| 4 | No token in repo/docs/logs | **Pass** |
| 5 | No `TELEGRAM_CHAT_ID` reuse for customer replies | **Pass** |
| 6 | `getMe` for both; different `bot_id` / `username` | **Pass** |
| 7 | No full Telegram Trigger workflow | **Pass** |

## n8n credentials (names only)

| n8n credential name | Role | `getMe` username |
|---------------------|------|------------------|
| `AlpsteinAIbot` | Owner notify (T13.5) | `@AlpsteinAibot` |
| `alpsteinai_0001bot` | Customer ingress (T14) | `@alpsteinai_0001bot` |

## `getMe` summary (masked, no tokens)

| Credential | `bot_id` | `username` | `getMe` |
|------------|----------|------------|---------|
| `AlpsteinAIbot` | `***4264` | `@AlpsteinAibot` | ok |
| `alpsteinai_0001bot` | `***8404` | `@alpsteinai_0001bot` | ok |

## Separation

```text
SEPARATION_VERIFIED=yes
```

Different masked `bot_id` suffixes and usernames confirm distinct bots.

## Verification command

```bash
/opt/alpstein-ai/n8n/scripts/t14-verify-telegram-bots.sh
```

Env overrides (optional): `TELEGRAM_OWNER_CRED_NAME`, `TELEGRAM_CUSTOMER_CRED_NAME`.

## T14.3 gate

**T14.3 MAY START** — bind `alpsteinai_0001bot` on Telegram Trigger/Send nodes when building ingress; do not use `AlpsteinAIbot` or `TELEGRAM_CHAT_ID` for customer path.

## Note (T13.5)

Repo workflow export may still reference legacy credential label `Telegram account`. Active n8n instance uses `AlpsteinAIbot`. Re-bind **Telegram Owner Notify** in n8n UI if owner notify regresses — **no workflow JSON change in T14.2**.
