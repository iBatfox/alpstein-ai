# T12.5 — Webhook response schemas (lead + notification)

**Status:** done (2026-05-24)

**Goal:** Extend `POST /api/v1/webhook/message` success `data` with typed lead/notification objects; no business logic wiring.

**Delivered:**

- `app/schemas/webhook_response.py` — `WebhookLeadSummary`, `WebhookNotificationPayload`, `WebhookMessageResponseData`, envelope + `serialize_webhook_message_success`
- `WebhookMessageProcessResult` extended with `lead_updated`, optional `lead`, optional `notification` (defaults preserve T11 behavior)
- Route uses schema serializer (`exclude_none` for optional objects)
- `tests/test_webhook_response_schemas.py`
- Route/integration tests updated for `lead_updated: false`

**Not in scope (T12.6):** LeadService wiring, notification policy population, fake placeholder data in routes.

**Next:** T12.6 — wire lead + notify into `WebhookMessageService`.
