@alpstein-backend-engineer

Implement T11.3 follow-up fixes only.

Goal:
Fix tenant/business consistency validation in deterministic dev AI seed.

Requirements:
- when reusing existing Business by external_id:
  - verify business.tenant_id matches resolved demo tenant.id
  - fail explicitly on mismatch

- when reusing rows by fixed UUID:
  - verify tenant_id and business_id match expected scope
  - for channel settings also verify channel
  - for knowledge sources also verify title/business scope where appropriate
  - fail explicitly on inconsistent existing rows

Rules:
- do not silently reuse cross-tenant data
- keep seed deterministic and idempotent
- no migrations
- no schema changes
- no AI execution
- no Prompt Builder
- no Gateway
- no route/webhook changes

Tests:
- existing business with wrong tenant fails
- existing fixed UUID row with wrong tenant/business fails
- valid deterministic rerun still succeeds

After implementation:
- run tests
- update completed.md if needed
- update current-state.md only if relevant
- stop for review