**Doc status:** canonical (shipped changelog)  
**Tier:** project-status/current

# Completed

- T-f2.3 ERPNext Lead conversation history: added `Communication` custom fields, Lead tab/HTML field `История переписки`, Lead client script chat renderer, n8n Communication read-model sync after ERPNext Lead sync, and runtime backfill for existing Ivan Instagram/Telegram messages from PostgreSQL source rows.
- BCB Phase 3.1: AI next-question generation for Business Context Builder — `BusinessContextBuilderPromptService` + `BusinessContextBuilderAiService` (reuses `AiGatewayService`); `save_user_message` stores user message then AI assistant reply with static fallback when `BCB_AI_ENABLED=false`, API key missing, or gateway failure; `BCB_AI_ENABLED` config; structured logs with `feature=business_context_builder`, `operation=next_question`; isolated `business_context_builder` schema only (no production assistant tables); complete session still uses placeholder draft result; tests in `test_business_context_builder_{prompt,ai,service,api}.py` (26 BCB-focused tests).
- Implemented minimal `GET /api/v1/health` backend endpoint.
- Implemented PostgreSQL foundation slice with async SQLAlchemy config/session/base, Tenant and Business models, Alembic init, and initial tenants/businesses migration.
- Implemented customers persistence slice with Customer model, customers Alembic migration, and focused model/migration tests.
- Implemented conversations persistence slice with Conversation model, conversations Alembic migration, and focused model/migration tests.
- Implemented messages persistence slice with Message model, messages Alembic migration, and focused model/migration tests.
- Implemented T2 domain exception `TenantContextError` in `app/exceptions.py` with focused pytest coverage.
- Implemented T3 tenant context validator in `app/services/tenant_context_validator.py` with focused pytest coverage (business/conversation/customer consistency; no API or DB access).
- Implemented T4 `MessageService.find_by_external_id` in `app/services/message_service.py` with focused pytest coverage (tenant/business-scoped lookup; no insert, API, or webhook).
- Implemented T5 `MessageService.save_incoming_customer_message` with tenant validation, idempotency via `find_by_external_id`, and focused pytest coverage (no API, webhook, AI, or lead logic).
- Implemented T6 partial unique index on `messages(business_id, external_message_id)` where `external_message_id IS NOT NULL`, with Alembic revision `0005`, Message model `__table_args__` update, and focused migration/model tests (no service, API, or webhook changes).
- Implemented T7 `BusinessService.get_by_external_id` in `app/services/business_service.py` and `BusinessNotFoundError` in `app/exceptions.py` with focused pytest coverage (lookup by globally unique `external_id`; no API or webhook).
- Implemented T8 `CustomerService.get_or_create_customer` and `ConversationService.get_or_create_open_conversation` with tenant-scoped lookup/create and focused pytest coverage (no API, webhook, AI, or lead logic).
- Implemented T9 normalized webhook Pydantic schemas in `app/schemas/webhook.py` per `specs/api/webhooks.md` with focused pytest coverage (validation only; no route or service wiring).
- Implemented T8.1 `ConversationService` reusable-status lookup (`open`, `waiting_for_customer`, `waiting_for_owner`; exclude `closed`/`archived`; order by `last_message_at` then `created_at`) per updated `specs/flows/incoming-message-flow.md`.
- Implemented T10 `POST /api/v1/webhook/message` route and `WebhookMessageService` orchestration (T5–T9 services; stub `reply_to_customer`; no AI/lead/notification).
- Implemented T11.1 AI configuration and `prompt_runs` database foundation: Alembic revision `0006` for `tenant_business_profiles`, `tenant_ai_profiles`, `tenant_knowledge_sources`, `tenant_channel_settings`, `prompt_templates`, and `prompt_runs`; matching SQLAlchemy models and focused migration/model tests (no AI services, seed data, or repositories).
- Implemented T11.2 tenant-scoped AI configuration read services (`TenantBusinessProfileService`, `TenantAiProfileService`, `TenantChannelSettingService`, `TenantKnowledgeSourceService`, `PromptTemplateService`) with focused pytest coverage (no orchestration, gateway, or routes).
- Implemented T11.3 development seed for AI configuration (`app/seed/dev_ai_configuration.py`, `scripts/seed_dev_ai_configuration.py`): deterministic demo tenant/business (`demo_barbershop_001`), platform `prompt_templates`, tenant profiles, channel settings, and knowledge sources; idempotent and blocked in production; focused pytest coverage (no AI execution, migrations, or webhook changes).
- Hardened T11.3 dev seed with explicit `DevAiConfigurationSeedError` on cross-tenant or mismatched fixed-UUID reuse (business `tenant_id`, profile scope, channel, knowledge title).
- Implemented T11.4 `AiConfigurationService` with typed `AiConfigurationBundle` DTOs (`app/schemas/ai_configuration.py`): loads tenant business profile, AI profile, channel settings, and active platform `prompt_templates` via T11.2 read services; enforces platform-over-tenant boundary (system prompt only on platform DTO; scrubs forbidden metadata keys); `PromptTemplateNotFoundError` when template missing; safe defaults for optional missing tenant config; focused pytest coverage (no gateway, webhook, or DB writes).
- Implemented T11.5 `KnowledgeRetrievalService` with `KnowledgeSnippet` / `KnowledgeRetrievalResult` DTOs (`app/schemas/knowledge.py`): uses `TenantKnowledgeSourceService` for tenant/business-scoped active sources; optional token-based text ranking; bounded snippet count and content length; defensive inactive/scope checks; focused pytest coverage (no vectors, embeddings, provider calls, prompt builder, or DB writes).
- Implemented T11.7 `PromptBuilderService` with `AssembledPrompt` / `AssembledPromptSection` DTOs (`app/schemas/assembled_prompt.py`): assembles eight canonical provider-neutral sections for `reply_to_customer` from `AiConfigurationBundle`, `KnowledgeRetrievalResult`, `ConversationHistory`, and current customer text; platform task registry; reference-data labeling; MVP character budgeting and deterministic truncation per `specs/architecture/prompt-builder-rules.md`; focused pytest coverage (no gateway, DB, or provider calls).
- Implemented T11.8 `AiGatewayService` with `AiGatewayResult` DTO (`app/schemas/ai_gateway.py`): maps `AssembledPrompt` to OpenAI chat completions inside `app/services/ai_gateway/_openai.py` only; `HttpxOpenAIChatClient` with `OPENAI_API_KEY`, `OPENAI_MODEL`, `AI_REQUEST_TIMEOUT`; normalized success/error results (timeout, empty text, HTTP errors, missing API key); injectable mock client for tests; focused pytest coverage (no DB, PromptRun, or webhook changes).
- Implemented T11.6 conversation history loading: `ConversationHistory` / `ConversationHistoryMessage` DTOs (`app/schemas/conversation_context.py`) and `MessageService.load_recent_conversation_history` (tenant/business/conversation-scoped; last 10–20 messages; oldest → newest; prompt-safe fields only); focused pytest coverage (no prompt builder, gateway, or DB writes).
- Expanded T11.4 pytest coverage for mis-scoped tenant rows (`TenantContextError`), channel mismatch, and metadata scrub on `business_context` and `channel_rules`.
- Implemented T11.9 `MessageService.save_outgoing_ai_message`: persists outgoing AI replies (`sender_type=ai`, `direction=outgoing`, `message_type=text`) with tenant/business/conversation validation via `validate_tenant_context`; optional `ai_metadata`; no `external_message_id` or `raw_payload`; `session.add` + `flush` only (no commit); focused pytest coverage (no PromptRun, gateway, prompt builder, or webhook changes).
- Implemented T12.1 `leads` database foundation: Alembic revision `0007`, `Lead` model with spec fields/indexes/FKs, status/priority constants, and focused migration/model tests (no LeadService, webhook wiring, or notification logic).
- Implemented T11.10 `PromptRunService.create_prompt_run` (`app/services/prompt_run_service.py`): persists AI execution audit rows with tenant/business/conversation/message scope validation; success and failure paths (`result` / `error`); token, latency, model, provider, template id/version fields; secret redaction on `final_prompt`, `result`, `error`, and string metadata; `final_prompt` capped at 16k chars with truncation marker; `session.add` + `flush` only (no commit); focused pytest coverage (no gateway, prompt builder, webhook, or API exposure of `final_prompt`).
- Implemented T11.11 `AiReplyOrchestrationService.generate_reply` (`app/services/ai_reply_orchestration_service.py`, `app/schemas/ai_reply.py`): chains AI Configuration → Knowledge Retrieval → conversation history → Prompt Builder → AI Gateway → PromptRun logging; returns `AiReplyResult` (`text`, `is_success`, `prompt_run_id`, `model`, `provider`, `error`) without exposing `final_prompt`; PromptRun written on gateway success and failure; injectable dependencies; focused pytest with mocked services (no outgoing message save, webhook, fallback, or direct OpenAI).
- Implemented T11.12 `AiReplyFallbackService.decide` (`app/services/ai_reply_fallback_service.py`, `app/schemas/ai_fallback.py`): pure policy for failed/empty AI replies — success bypasses fallback; otherwise `tenant_ai_profiles.fallback_response` then platform default; `should_handoff` from `handoff_enabled`; returns `FallbackDecision` (`should_reply`, `fallback_text`, `should_handoff`, `reason`); no provider calls, DB writes, or webhook integration; focused pytest coverage.
- Implemented T11.13 `AiReplyOrchestrationCoordinator.execute_for_incoming_message` (`app/services/ai_reply_orchestration_coordinator.py`, `app/schemas/ai_reply_orchestration.py`): when `is_duplicate` from incoming message save, skips `AiReplyOrchestrationService` (no Prompt Builder, Gateway, or PromptRun); returns `AiReplyOrchestrationOutcome` (`is_duplicate`, `ai_executed`, `reason`, optional `ai_reply`); non-duplicate path unchanged; focused pytest coverage (no webhook, outgoing message, or n8n changes).
- Implemented T11.14 webhook AI wire-up in `WebhookMessageService`: replaces stub `reply_to_customer` with coordinator → success text or `AiReplyFallbackService` fallback; `save_outgoing_ai_message` on sent replies; duplicate path uses `find_last_outgoing_ai_message` or safe acknowledgment (no new AI/PromptRun/outgoing row); `lead_created`/`notify_owner` remain false; route envelope unchanged; `MessageService.find_last_outgoing_ai_message` added; focused pytest in `test_webhook_message_ai_wiring.py`.
- Implemented T11.15 AI webhook integration/regression suite (`tests/test_t11_ai_webhook_integration.py`): wires `WebhookMessageService` through coordinator, orchestration, configuration, knowledge, prompt builder, mocked gateway, PromptRun, and outgoing message persistence; covers success, gateway failure + fallback, duplicate skip, API leakage protection, tenant scope, and thin route module check (183 tests green).
- T11.7 naming/spec sync: canonical Prompt Builder section IDs `tenant_behavior` and `knowledge` (aligned with `specs/architecture/prompt-builder-rules.md`).
- T11.16 project status/docs consistency sweep: updated `current-state.md`, `next-steps.md`, `t11-ai-orchestration-plan.md`, and `tasks/done/t11-ai-orchestration-slice.md` to reflect T11.1–T11.15 completion; T10-F1 documented as production blocker before n8n; T12 as next feature slice.
- Implemented T10-F1 webhook API token auth: `require_webhook_token` dependency on `POST /api/v1/webhook/message` (`X-Alpstein-Webhook-Token` vs `N8N_BACKEND_API_TOKEN`); `WebhookAuthError` handler with standard error envelope (`UNAUTHORIZED` / `FORBIDDEN`); `.env.example` placeholder; `tests/test_webhook_token_auth.py` + route test auth headers (**187** tests green).
- Implemented T12.2 `LeadService` (`app/services/lead_service.py`): `find_active_lead` (tenant/business/customer/conversation scope; statuses `new`/`in_progress`/`contacted`; latest by `updated_at`/`created_at`), `create_lead` (defaults `status=new`, `priority=normal`, `add`+`flush` only), `update_lead` (allowed MVP fields + `updated_at`; tenant/business scope via `TenantContextError`); focused pytest in `tests/test_lead_service.py` (**204** tests green).
- Implemented T12.3 `NotificationPolicyService` (`app/services/notification_policy_service.py`, `app/schemas/notification.py`): pure `NotificationDecision` policy from `is_duplicate`, `urgent_detected`, `handoff_requested`, `ai_failed`, `lead_created`, `lead_updated`; deterministic priority `urgent_lead` → `human_handoff` → `ai_failure` → `new_lead`; `lead_updated` only suppresses notify; focused pytest in `tests/test_notification_policy_service.py` (**214** tests green).
- Implemented T12.4 `LeadSignalDetectionService` (`app/services/lead_signal_detection_service.py`, `app/schemas/lead_signal.py`): case-insensitive MVP keyword heuristics for `urgent_detected` / `handoff_requested` on customer message text; word-boundary single tokens; documented false-positive risk; focused pytest in `tests/test_lead_signal_detection_service.py` (**223** tests green).
- Implemented T12.6 webhook lead/notification wire-up in `WebhookMessageService`: duplicate path skips lead/notify; non-duplicate create/update via `LeadService` with tenant validation; `LeadSignalDetectionService` + `NotificationPolicyService`; `ai_failed` only on fallback reply; response `lead_created`/`lead_updated`/`notify_owner`/optional `lead`/`notification`; tests in `tests/test_webhook_message_lead_wiring.py` (**244** tests green).
- Implemented T12.7 webhook HTTP regression hardening: `TenantContextError` → HTTP 400 `VALIDATION_ERROR` on webhook route; `tests/test_t12_webhook_http_regression.py`; strengthened `tests/test_t11_ai_webhook_integration.py` (duplicate lead flags, `ai_failure` notification, urgent active-lead); **252** tests green.
- Implemented T12.5 webhook success response schemas (`app/schemas/webhook_response.py`): `WebhookLeadSummary`, `WebhookNotificationPayload`, `WebhookMessageResponseData`; `lead_updated` on `data`; optional `lead` / `notification` omitted when null; route uses `serialize_webhook_message_success`; `WebhookMessageProcessResult` extended with defaults (no LeadService/notify wiring); `tests/test_webhook_response_schemas.py`; route/T11 envelope helpers updated.
- T13.1 n8n env/credential checklist: [`docs/ops/n8n-env-credential-checklist.md`](../ops/n8n-env-credential-checklist.md) — backend connection, token alignment, n8n credential placement, test payload requirements, security rules, manual verification (names only, no secrets).
- T13.2 n8n Workflow 1 skeleton: [`n8n/workflows/t13_workflow1_test_webhook_skeleton.json`](../../n8n/workflows/t13_workflow1_test_webhook_skeleton.json) + [`docs/ops/n8n-workflow1-test-webhook.md`](../ops/n8n-workflow1-test-webhook.md) — test webhook, normalize to backend contract, normalized preview response; disabled HTTP placeholder for T13.3 (no backend call, no secrets).
- T13.3 n8n backend HTTP wiring: updated workflow export — enabled **POST Backend** with `$env.BACKEND_BASE_URL` + `$env.N8N_BACKEND_API_TOKEN` header; Normalize → HTTP → Respond (backend envelope passthrough); safe error branch on non-2xx (`N8N_BACKEND_REQUEST_FAILED`); docs + Gate 1 checklist (no retries, no Telegram, no backend changes).
- T13.0-impl Alpstein n8n Docker runtime: `n8n/docker-compose.yml` (project `alpstein-n8n`, container `alpstein_n8n`, `127.0.0.1:15679`, volume `alpstein_n8n_data`), `n8n/.env.example`, `.gitignore`, [`docs/ops/n8n-runtime-start.md`](../ops/n8n-runtime-start.md) — no HubSpot n8n changes.
- n8n HTTPS reverse proxy: nginx site `/etc/nginx/sites-available/n8n.alpstein-ai.ch` → `https://n8n.alpstein-ai.ch` (Let's Encrypt, HTTP→HTTPS redirect, WebSocket headers); upstream `127.0.0.1:15679` only; HubSpot `n8n.conf`/`15678` untouched; [`docs/ops/n8n-https-reverse-proxy.md`](../ops/n8n-https-reverse-proxy.md).
- T13 Gate 1 runtime pass (docs sign-off): HTTPS test webhook `https://n8n.alpstein-ai.ch/webhook-test/alpstein/test/incoming` → backend via `BACKEND_BASE_URL=http://172.20.0.1:8010` (UFW `172.20.0.0/16` → host `8010`, backend on `0.0.0.0:8010`); response `success: true` with non-empty `reply_to_customer`; duplicate resend `message.is_duplicate: true` — [`docs/ops/n8n-workflow1-test-webhook.md`](../ops/n8n-workflow1-test-webhook.md), [`tasks/todo/t13-n8n-workflow-slice.md`](../../tasks/todo/t13-n8n-workflow-slice.md).
- T13.4 n8n customer reply shaping: workflow export — **Shape Customer Reply** (Code) between **POST Backend** and **Respond Customer Reply**; test webhook returns `{ success, reply_to_customer, message?: { is_duplicate } }` only (no lead/notify/raw envelope); error branch unchanged (`N8N_BACKEND_REQUEST_FAILED` / 502); manual test passed on `http://127.0.0.1:15679/webhook/alpstein/test/incoming`; [`docs/ops/n8n-workflow1-test-webhook.md`](../ops/n8n-workflow1-test-webhook.md) § T13.4.
- T13.5 n8n owner notification branch: **IF Notify Owner** (conditions on `$('POST Backend')`: `notify_owner`, `notification`, not duplicate) → **Shape Owner Notification** → **Telegram Owner Notify** (`telegramApi` placeholder credential, `$env.TELEGRAM_CHAT_ID`, `continueOnFail`); runs after **Respond Customer Reply** (webhook-safe); T13.4 path unchanged; export `t13-5-owner-notify-v4`; [`docs/ops/n8n-workflow1-test-webhook.md`](../ops/n8n-workflow1-test-webhook.md) § T13.5.
- T13.5 runtime verification (final, 2026-05-25): active workflow `2qfhWKtgbDy6YeTh`; Gate 2 **passed** — urgent owner Telegram **delivered** (exec **50**); duplicate suppression (exec **51**); normal follow-up no notify (exec **52**); `TELEGRAM_CHAT_ID` + credential `Telegram account` bound — [`docs/ops/n8n-workflow1-test-webhook.md`](../ops/n8n-workflow1-test-webhook.md).
- HF-1 History Safety: §7 preamble + non-authoritative `ai` labels; current context overrides stale assistant facts — [`tasks/done/HF-1-history-safety-promptbuilder.md`](../../tasks/done/HF-1-history-safety-promptbuilder.md), `history_safety_prompt_instructions.py`, `tests/test_history_safety_prompt_builder.py`.
- P0 global Alpstein contamination fix (`8ad8489`): business-aware §2 gating via `alpstein_product_behavior_enabled_for_business()`; `PRE_SALES_TASK_APPENDIX` no longer assembled at runtime; non-Alpstein tenants use generic greeting only — [`tasks/done/P0-global-alpstein-contamination-fix.md`](../../tasks/done/P0-global-alpstein-contamination-fix.md), `tests/test_product_behavior_gating.py`.
- P1 legacy appendix removal: deleted `PRE_SALES_TASK_APPENDIX` from backend; §2 is charter + intent (Alpstein) or core task + generic greeting (non-Alpstein) — [`tasks/done/P1-remove-legacy-pre-sales-task-appendix.md`](../../tasks/done/P1-remove-legacy-pre-sales-task-appendix.md).
- Documentation topology Phase 1.2: tier scaffolding, [`docs/README.md`](../README.md), [`docs/MIGRATION-PLAN.md`](../MIGRATION-PLAN.md), [`documentation-topology-status.md`](documentation-topology-status.md), canonicality markers on priority docs — no bulk file moves.
- Phase B2 deployment portability (B2.0–B2.9): deployment contract, env templates, root `docker-compose.yml` (postgres + backend + n8n), `backend/Dockerfile`, migrate-then-serve entrypoint, readiness endpoint, bootstrap profile, clean-clone gate — evidence in `tasks/done/T-b2.*.md`, [`docs/deployment/`](../deployment/).
- Phase D4 operational verification (D4.1–D4.4, U1, OPS-C1): compose E2E hardening, DB replay lineage, production-safe observability envelope, JSON-safe `prompt_runs.metadata`, single ingress policy — [`docs/audits/d4-operational-wrap-up-2026-05-27.md`](../audits/d4-operational-wrap-up-2026-05-27.md), slice reports `d4-1` through `d4-4`, [`docs/ops/operational-ingress-policy.md`](../ops/operational-ingress-policy.md).
- E0 repository layout (n8n scripts): documented ownership for `n8n/`, `n8n/scripts/`, `scripts/n8n/`, `scripts/verify/` — closed **TD-D4-n8n-script-layout** with no file moves — [`docs/ops/repository-layout-n8n-scripts.md`](../ops/repository-layout-n8n-scripts.md).
- INC-2026-05-29 Telegram ingress outage (resolved): documented dual root cause (missing Telegram webhook registration + n8n 2.x `$env` block); remediation via webhook re-bind and `N8N_BLOCK_ENV_ACCESS_IN_NODE=false`; validation execution **313**, backend webhook **200** — [`docs/audits/telegram-incident-2026-05-29.md`](../audits/telegram-incident-2026-05-29.md), runbook §10 in [`n8n-unified-customer-ingress-runbook.md`](../ops/n8n-unified-customer-ingress-runbook.md).
